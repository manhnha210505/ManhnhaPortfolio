# Blog (Deferred — Not in v1 Scope)

Per `foundation/ProjectVision.md` non-goals: this site is not a full CMS/blogging platform for v1. This file is a placeholder for future scope only.

## If revisited later
- Would need its own content model in Supabase (separate from `projects`/`contacts`).
- Would need its own SEO/structured data pass (`engineering/SEO.md`).
- Should not be started until core sections (Hero → Contact) are complete and stable — avoid scope creep into `spec.md`/`plan.md` before v1 ships (`governance/SpecKitRules.md`).
